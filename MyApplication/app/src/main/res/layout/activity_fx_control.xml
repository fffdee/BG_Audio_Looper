<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark"
    android:fitsSystemWindows="true">

    <!-- 统一顶部导航栏 -->
    <include layout="@layout/include_base_toolbar" />

    <!-- FX 操作按钮栏：全部效果总开关 + 保存 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end|center_vertical"
        android:background="@color/bg_card"
        android:paddingStart="@dimen/toolbar_padding"
        android:paddingEnd="@dimen/toolbar_padding"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="全部效果"
            android:textSize="14sp"
            android:textColor="@color/text_primary"
            android:layout_marginEnd="8dp" />

        <Switch
            android:id="@+id/sw_all_fx"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp" />

        <ImageButton
            android:id="@+id/btn_save_fx"
            android:layout_width="@dimen/icon_size_large"
            android:layout_height="@dimen/icon_size_large"
            android:layout_marginEnd="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_save"
            android:tint="@color/text_accent"
            android:contentDescription="保存效果设置"
            android:scaleType="centerInside"
            android:padding="8dp" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <!-- 各效果卡片由 FxControlActivity 动态生成：
             每张卡片 = 标题 + bypass 开关 + 该效果的参数滑条组 -->
        <LinearLayout
            android:id="@+id/fx_cards_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/toolbar_padding" />
    </ScrollView>

</LinearLayout>
